<?xml version="1.0" encoding="utf-8"?>
<!--
  Release-build alternative to android:usesCleartextTraffic="true".

  Copy to gen/android/app/src/main/res/xml/network_security_config.xml and
  point the <application> tag at it:

    android:networkSecurityConfig="@xml/network_security_config"

  Cleartext is permitted only on the private address ranges a LAN game uses,
  so the app still cannot make an unencrypted connection to the internet.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
