{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "net",
  "sourceRoot": "packages/net/src",
  "projectType": "library",
  "tags": ["layer:net"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/net" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p packages/net/tsconfig.json" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/net" } }
  }
}