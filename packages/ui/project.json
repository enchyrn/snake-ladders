{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "ui",
  "sourceRoot": "packages/ui/src",
  "projectType": "library",
  "tags": ["layer:ui"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/ui" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p packages/ui/tsconfig.json" } },
    "test": { "executor": "nx:run-commands", "dependsOn": [{ "projects": ["game-web"], "target": "panda" }], "options": { "command": "vitest run packages/ui" } }
  }
}