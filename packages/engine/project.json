{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "engine",
  "sourceRoot": "packages/engine/src",
  "projectType": "library",
  "tags": ["layer:engine"],
  "targets": {
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p packages/engine/tsconfig.json" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/engine/src/**/*.test.ts" } }
  }
}