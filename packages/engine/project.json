{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "engine",
  "sourceRoot": "packages/engine/src",
  "projectType": "library",
  "tags": ["layer:engine"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/engine" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p packages/engine/tsconfig.json" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/engine" } }
  }
}