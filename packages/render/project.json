{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "render",
  "sourceRoot": "packages/render/src",
  "projectType": "library",
  "tags": ["layer:render"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "eslint packages/render" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc --noEmit -p packages/render/tsconfig.json" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run packages/render" } }
  }
}